<nav id="nav">
  <ul>
  {%- for nav in collections.nav | sort(attribute="date") -%}
    <li{% if nav.url == page.url %} class="current"{% endif %}>
      <a href="{{ nav.url | url }}"{% if nav.url == page.url %}data-current="current item"{% endif %}>{{ nav.data.navtitle }}</a>
    </li>
  {%- endfor -%}
  </ul>
</nav>
